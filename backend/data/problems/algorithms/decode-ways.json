{
  "id": "decode-ways",
  "title": "Decode Ways",
  "category": "algorithms",
  "difficulty": "medium",
  "description": "A message containing letters from `A-Z` can be **encoded** into numbers using the following mapping:\n\n```\n'A' -> \"1\"\n'B' -> \"2\"\n...\n'Z' -> \"26\"\n```\n\nTo **decode** an encoded message, all the digits must be grouped then mapped back into letters using the reverse of the mapping above (there may be multiple ways).\n\nGiven a string `s` containing only digits, return the **number of ways** to decode it. The answer is guaranteed to fit in a 32-bit integer.",
  "examples": [
    {
      "input": "s = \"12\"",
      "output": "2",
      "explanation": "\"12\" could be decoded as \"AB\" (1 2) or \"L\" (12)."
    },
    {
      "input": "s = \"226\"",
      "output": "3",
      "explanation": "\"226\" could be decoded as \"BZ\" (2 26), \"VF\" (22 6), or \"BBF\" (2 2 6)."
    },
    {
      "input": "s = \"06\"",
      "output": "0",
      "explanation": "\"06\" cannot be mapped to \"F\" because of the leading zero. Since there is no character that maps to 06, there are no valid decodings."
    }
  ],
  "constraints": [
    "1 <= len(s) <= 100",
    "s contains only digits and may contain leading zeros."
  ],
  "starter_code": "def num_decodings(s: str) -> int:\n    # Write your solution here\n    pass\n",
  "test_cases": [
    {
      "input": "num_decodings('12')",
      "expected": "2",
      "is_hidden": false
    },
    {
      "input": "num_decodings('226')",
      "expected": "3",
      "is_hidden": false
    },
    {
      "input": "num_decodings('06')",
      "expected": "0",
      "is_hidden": false
    },
    {
      "input": "num_decodings('10')",
      "expected": "1",
      "is_hidden": true
    },
    {
      "input": "num_decodings('27')",
      "expected": "1",
      "is_hidden": true
    },
    {
      "input": "num_decodings('11106')",
      "expected": "2",
      "is_hidden": true
    }
  ],
  "time_limit_minutes": 25,
  "tags": [
    "dynamic-programming",
    "string"
  ],
  "solution": "# Problem: Given a string of digits, count the number of ways to decode it\n# into letters where A=1, B=2, ..., Z=26.\n#\n# Approach: Dynamic programming. At each position i, the number of decodings\n# depends on whether the current digit (and optionally the previous digit)\n# form valid letter mappings.\n# - If s[i] != '0', it can be decoded as a single digit: dp[i] += dp[i-1]\n# - If s[i-1:i+1] is between \"10\" and \"26\", the two-digit pair is valid: dp[i] += dp[i-2]\n# We only need the last two DP values, so we use two variables instead of an array.\n#\n# Time complexity: O(n) where n = len(s)\n# Space complexity: O(1)\n#\n# Key steps:\n# 1. Handle edge case: if s starts with '0', return 0 immediately\n# 2. Initialize two variables representing dp[i-2] and dp[i-1]\n# 3. For each position, accumulate ways from single-digit and two-digit decodings\n# 4. Return the final count\n\ndef num_decodings(s: str) -> int:\n    if not s or s[0] == '0':\n        return 0\n\n    # prev2 = dp[i-2], prev1 = dp[i-1]\n    prev2, prev1 = 1, 1\n\n    for i in range(1, len(s)):\n        current = 0\n\n        # Single digit decode: valid if current digit is not '0'\n        if s[i] != '0':\n            current += prev1\n\n        # Two digit decode: valid if the pair is between 10 and 26\n        two_digit = int(s[i - 1:i + 1])\n        if 10 <= two_digit <= 26:\n            current += prev2\n\n        prev2, prev1 = prev1, current\n\n    return prev1"
}
