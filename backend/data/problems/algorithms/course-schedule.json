{
  "id": "course-schedule",
  "title": "Course Schedule",
  "category": "algorithms",
  "difficulty": "medium",
  "description": "There are a total of `num_courses` courses you have to take, labeled from `0` to `num_courses - 1`. You are given a list of `prerequisites` where `prerequisites[i] = [a, b]` indicates that you must take course `b` before course `a`.\n\nReturn `True` if you can finish all courses, or `False` if there is a circular dependency making it impossible.",
  "examples": [
    {
      "input": "num_courses = 2, prerequisites = [[1, 0]]",
      "output": "True",
      "explanation": "Take course 0 first, then course 1."
    },
    {
      "input": "num_courses = 2, prerequisites = [[1, 0], [0, 1]]",
      "output": "False",
      "explanation": "Circular dependency: 0 requires 1 and 1 requires 0."
    }
  ],
  "constraints": [
    "1 <= num_courses <= 2000",
    "0 <= len(prerequisites) <= 5000",
    "prerequisites[i].length == 2",
    "All prerequisite pairs are unique."
  ],
  "starter_code": "def can_finish(num_courses: int, prerequisites: list[list[int]]) -> bool:\n    # Write your solution here\n    pass\n",
  "test_cases": [
    {
      "input": "can_finish(2, [[1, 0]])",
      "expected": "True",
      "is_hidden": false
    },
    {
      "input": "can_finish(2, [[1, 0], [0, 1]])",
      "expected": "False",
      "is_hidden": false
    },
    {
      "input": "can_finish(4, [[1, 0], [2, 1], [3, 2]])",
      "expected": "True",
      "is_hidden": false
    },
    {
      "input": "can_finish(1, [])",
      "expected": "True",
      "is_hidden": true
    },
    {
      "input": "can_finish(3, [[0, 1], [1, 2], [2, 0]])",
      "expected": "False",
      "is_hidden": true
    },
    {
      "input": "can_finish(5, [[1, 0], [2, 0], [3, 1], [4, 2]])",
      "expected": "True",
      "is_hidden": true
    }
  ],
  "time_limit_minutes": 25,
  "tags": [
    "graph",
    "topological-sort",
    "dfs",
    "cycle-detection"
  ],
  "solution": "# Problem: Determine if all courses can be completed given prerequisite pairs.\n#          This is equivalent to detecting if a directed graph has a cycle.\n#\n# Approach: DFS-based cycle detection using 3-state node coloring.\n#   - WHITE (0): unvisited\n#   - GRAY  (1): currently being explored (on the recursion stack)\n#   - BLACK (2): fully processed, no cycle found through this node\n#   If we encounter a GRAY node during DFS, a cycle exists.\n#\n# Time complexity:  O(V + E) where V = num_courses, E = len(prerequisites)\n# Space complexity: O(V + E) for adjacency list and color array\n#\n# Key steps:\n#   1. Build an adjacency list from prerequisites\n#   2. For each unvisited node, run DFS\n#   3. If any DFS finds a back-edge (GRAY node), return False\n#   4. If all nodes processed without cycles, return True\n\ndef can_finish(num_courses: int, prerequisites: list[list[int]]) -> bool:\n    # Build adjacency list: course -> list of courses that depend on it\n    graph = [[] for _ in range(num_courses)]\n    for course, prereq in prerequisites:\n        graph[prereq].append(course)\n\n    # 0 = unvisited, 1 = in-progress, 2 = completed\n    color = [0] * num_courses\n\n    def has_cycle(node: int) -> bool:\n        color[node] = 1  # Mark as in-progress\n        for neighbor in graph[node]:\n            if color[neighbor] == 1:  # Back-edge: cycle detected\n                return True\n            if color[neighbor] == 0 and has_cycle(neighbor):\n                return True\n        color[node] = 2  # Mark as fully processed\n        return False\n\n    for course in range(num_courses):\n        if color[course] == 0:  # Only start DFS from unvisited nodes\n            if has_cycle(course):\n                return False\n\n    return True"
}
