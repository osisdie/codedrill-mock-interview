{
  "id": "fibonacci-number",
  "title": "Fibonacci Number",
  "category": "algorithms",
  "difficulty": "easy",
  "description": "The **Fibonacci numbers**, commonly denoted `F(n)`, form a sequence called the Fibonacci sequence, such that each number is the sum of the two preceding ones, starting from `0` and `1`. That is:\n\n- `F(0) = 0, F(1) = 1`\n- `F(n) = F(n - 1) + F(n - 2)`, for `n > 1`\n\nGiven `n`, calculate `F(n)`.",
  "examples": [
    {
      "input": "n = 2",
      "output": "1",
      "explanation": "F(2) = F(1) + F(0) = 1 + 0 = 1."
    },
    {
      "input": "n = 3",
      "output": "2",
      "explanation": "F(3) = F(2) + F(1) = 1 + 1 = 2."
    },
    {
      "input": "n = 4",
      "output": "3",
      "explanation": "F(4) = F(3) + F(2) = 2 + 1 = 3."
    }
  ],
  "constraints": [
    "0 <= n <= 30"
  ],
  "starter_code": "def fib(n: int) -> int:\n    # Write your solution here\n    pass\n",
  "test_cases": [
    {
      "input": "fib(2)",
      "expected": "1",
      "is_hidden": false
    },
    {
      "input": "fib(3)",
      "expected": "2",
      "is_hidden": false
    },
    {
      "input": "fib(4)",
      "expected": "3",
      "is_hidden": false
    },
    {
      "input": "fib(0)",
      "expected": "0",
      "is_hidden": true
    },
    {
      "input": "fib(1)",
      "expected": "1",
      "is_hidden": true
    },
    {
      "input": "fib(10)",
      "expected": "55",
      "is_hidden": true
    }
  ],
  "time_limit_minutes": 15,
  "tags": [
    "dynamic-programming",
    "recursion"
  ],
  "solution": "# Problem: Compute the nth Fibonacci number where F(0)=0, F(1)=1,\n#          and F(n) = F(n-1) + F(n-2) for n > 1.\n#\n# Approach: Iterative bottom-up computation using two variables.\n#   Instead of storing the entire sequence or using recursion,\n#   we keep only the two most recent values and update them\n#   as we iterate from 2 to n.\n#\n# Time complexity:  O(n)\n# Space complexity: O(1)\n#\n# Key steps:\n#   1. Handle base cases: n == 0 -> 0, n == 1 -> 1\n#   2. Initialize two variables for F(0) and F(1)\n#   3. Iterate from 2 to n, updating the two variables\n#   4. Return the latest value\n\ndef fib(n: int) -> int:\n    if n <= 1:\n        return n\n\n    prev, curr = 0, 1\n    for _ in range(2, n + 1):\n        prev, curr = curr, prev + curr  # shift window forward\n\n    return curr"
}
